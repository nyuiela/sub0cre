# CRE WORKFLOW SETTINGS — market-discovery
# Autonomous market creation: fetch draft markets from backend, create on-chain, notify registry-sync.

staging-settings:
  user-workflow:
    workflow-name: "sub0cre-market-discovery"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.staging.json"
    secrets-path: "../secrets.yaml"

docker-settings:
  user-workflow:
    workflow-name: "sub0cre-market-discovery"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.docker.json"
    secrets-path: "../secrets.yaml"

production-settings:
  user-workflow:
    workflow-name: "sub0cre-market-discovery"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.production.json"
    secrets-path: "../secrets.yaml"
