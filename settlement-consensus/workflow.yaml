# CRE WORKFLOW SETTINGS — settlement-consensus
# Two-agent LLM deliberation inside CRE TEE; writes resolution report on-chain.

staging-settings:
  user-workflow:
    workflow-name: "sub0cre-settlement-consensus"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.staging.json"
    secrets-path: "../secrets.yaml"

docker-settings:
  user-workflow:
    workflow-name: "sub0cre-settlement-consensus"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.docker.json"
    secrets-path: "../secrets.yaml"

production-settings:
  user-workflow:
    workflow-name: "sub0cre-settlement-consensus"
  workflow-artifacts:
    workflow-path: "./main.ts"
    config-path: "./config.production.json"
    secrets-path: "../secrets.yaml"
